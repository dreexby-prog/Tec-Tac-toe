#include "buttonVar.hpp"

std::vector<Choosable> menu_buttons(3, Choosable(pic2, pic3));

std::vector<Choosable> chMintRose = {Choosable(chMint_texture[0],chMint_texture[1]), Choosable(chRose_texture[0], chRose_texture[1])};
